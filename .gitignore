# Terraform
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/*.tfstate
terraform/*.tfstate.*
terraform/errored.tfstate
terraform/state.remote.backup.tfstate
terraform/crash.log
terraform/crash.*.log

# tfvars (often secrets)
terraform/*.tfvars

# kube/aws creds
.aws/
.kube/
*.pem
*accessKeys*.csv

# editor / OS
.DS_Store

# Local artifacts / backups
grading.json
terraform/*.bak.*
terraform/*.before-*
terraform/plan.txt
terraform/grading.json
terraform/evidence/*.txt
terraform/evidence/*.json

# local terraform plan output (can contain access key IDs)
terraform/plan.txt

# Terraform plan output (can contain access key IDs)
terraform/plan.txt
.env
.env.*

# Env files
.env
.env.*

# Env files
.env
.env.*
